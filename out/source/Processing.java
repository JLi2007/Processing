/* autogenerated by Processing revision 1293 on 2024-09-10 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class Processing extends PApplet {

// Moving lines with if, else if, else statement
// September 10, 2024
float a,b,i;
int c;
int rect_height = 5;
// initalize intlist to store random line values
IntList lines;

public void setup() {
    /* size commented out by preprocessor */;
    colorMode(HSB);
    c = PApplet.parseInt(random(255));
    a = height / 2;
    lines = new IntList();
}

public void draw() {
    background(50);
    fill(c,255,255);

    // first rectangle/"line"
    rect(0,a,width,rect_height);
    // maps more rectangles ("lines") 
    for (int j = 0; j < lines.size(); j++) {
        rect(0,lines.get(j) + a,width,rect_height);
    }
    // changing a value
    a = (a - 2);
    if (a < 0) {
        a = height;
    }
    
    // so color variable stays in range 0, 255
    if (c > 255) {c -= 255;} else if (c < 0) { c += 255;}  
    
    // random conditional color statements with a variable to add more lines with a random starting point
    // for the if/else if/else implmentation
    if (a == height / 2) {
        i = PApplet.parseInt(random(height));
        lines.append(PApplet.parseInt(i));
    }   
    else if (a == height / 3) {
        i = PApplet.parseInt(random(height));
        lines.append(PApplet.parseInt(i));
    }
    else if (a == height / 4) {
        i = PApplet.parseInt(random(height));
        lines.append(PApplet.parseInt(i));
    }
    else {
        c ++;
    } 
    c++;  
}


  public void settings() { size(800, 400); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "Processing" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
